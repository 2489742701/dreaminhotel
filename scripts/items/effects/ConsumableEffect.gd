# ==============================================================================
# ConsumableEffect.gd
# 消耗品效果基类
# 这是一个资源类型的基类，用于定义各种消耗品(如药水、食物等)使用时产生的效果
# 其他具体效果类需要继承此类并实现apply方法
# 适用于初学者：理解基类和继承的概念是面向对象编程的基础
# ==============================================================================

# 定义类名为ConsumableEffect，使其他脚本可以直接引用此类
class_name ConsumableEffect
# 继承自Resource类，表示这是一个可序列化的资源
# Resource类型的优点是可以在Godot编辑器中创建、保存和修改，方便游戏设计
# 这意味着你可以在不编写代码的情况下创建不同效果的消耗品
# 例如：创建一个生命值恢复药水资源，设置恢复量等属性
extends Resource

# ========== 通用属性 ==========

# 消耗品使用时播放的音效
# 在物品被使用时，系统会尝试播放这个音效
# 开发者可以在编辑器中拖放音效文件到这里
@export var 使用音效: AudioStream

# ========== 核心方法 ==========

# 应用效果的核心方法 - 这是一个抽象方法
# 所有继承自ConsumableEffect的具体效果类都必须实现此方法
# @param _target: 效果作用的目标节点，通常是玩家节点
# @return: void - 无返回值
# 初学者提示：当你创建一个继承自ConsumableEffect的新效果类时，
# 必须重写(override)这个apply方法来实现具体效果逻辑
func apply(_target: Node) -> void:
	# 基类中仅抛出错误，提醒开发者需要在子类中实现具体逻辑
	# 这是一个典型的抽象基类模式，如果有人直接使用这个基类而不是其子类，
	# Godot会显示错误信息，帮助开发者发现问题
	push_error("未实现具体逻辑")
	
# ========== 使用说明 ==========
# 如何使用这个基类创建新的消耗品效果：
# 1. 创建一个新的gd脚本文件
# 2. 让它继承自ConsumableEffect
# 3. 重写apply方法，实现具体效果
# 4. 可以添加额外的自定义属性
# 5. 在编辑器中创建此资源类型的实例并配置属性
# 6. 将该资源分配给Item资源的consumable_effect属性
